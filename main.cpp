#include <iostream>

#include "includes.inc"

int main() {
  for (int i = 0; i < 5; i++) {
    std::cout << "Hello, World!" << std::endl;
  }

  // Entry<std::string> entry = Entry<std::string>(&test);

  return 0;
}
